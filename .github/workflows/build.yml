name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Check quality
        run: make quality

      - name: Run tests
        run: make test


        # upload coverage
