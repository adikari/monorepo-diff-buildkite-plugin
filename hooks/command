#!/bin/bash
set -euo pipefail

version="2.0.0"
repo="https://github.com/chronotc/monorepo-diff-buildkite-plugin"
executable="monorepo-diff-buildkite-plugin"

curl -Lf -o buildpipe ${repo}/releases/download/v${version}/${executable} && chmod +x ${executable}

./${executable}
